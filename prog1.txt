# Program 1 by Group12 for Project3
# 28, Oct, 2018

init R0, 0
ld R2, R0       # R2 = P

init R0, 1
ld R1, R0       # R1 = Q

init R3, 3      # R3 = MUX[3] = 6, the result of calculation
init R0, 1      # the exponential counter

L1:
R3x6            # R3 = R3 * 6
inc R0
add R3, R1      # R3 = R3 + Q

L2:
subR3 R1      # R3 = R3 - Q
jpu1 R1, R3, L2  # jump to L2 if R1 < R3 (Q < Result) there should be Q<= Result
jpu1 R0, R2, L1  # jump to L1 if R0 < R2 (counter < P)

Finish:

jpu2 R3, R1, END # R3 should <= R1(Q) here, if R3 < R1 (Result < Q) jump to END
subR3 R1        # PC goes here only if R3 = R1 (Result = Q), when should subtract Q one more time
END:


